#include "main.h"
/**
 * mul - a function that multiplies two variables
 * @a: first character to be multiplied
 * @b: second character to be multiplied
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
