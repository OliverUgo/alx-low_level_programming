#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet_x10(void);
{
	char low_alph;

	for (low_alph = 'a'; low_alph <= 'z'; low_alph++)
	{	_putchar(low_alph);
	}
	_putchar('\n');
return (0);
}
